@page
@model CR_Proiect.Pages.Tari.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Tara[0].Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Tara[0].Suprafata)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Tara[0].Capitala)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Tara[0].Biserica.Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Tara[0].Biserica.Rege.Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Tara[0].Biserica.Regina.Name)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model.Tara)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Name)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Suprafata)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Capitala)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Biserica.Name)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Biserica.Rege.Name)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Biserica.Regina.Name)
                </td>
                <td>
                    <a asp-page="./Edit" asp-route-id="@item.ID">Edit</a> |
                    <a asp-page="./Details" asp-route-id="@item.ID">Details</a> |
                    <a asp-page="./Delete" asp-route-id="@item.ID">Delete</a>
                </td>
            </tr>
        }
    </tbody>
</table>
